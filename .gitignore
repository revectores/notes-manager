__pycache__/
*.pyc
logs/
.DS_Store

# Leftovers from the Markdown era, kept only so the migration can be re-run or
# checked against. The notes themselves now live in the store outside this repo
# (see AGENTS.md); nothing here reads either of these any more.
assets/search-index.json
assets/search-index.json.bak
notes-html/
